{
  "name": "mural",
  "version": "1.0.0",
  "description": "Mural de cartoes",
  "main": "babel.config.js",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1sd",
    "start":"./note-modules/.bin/babel src --out-dir dist --copy-files --presets@babel/presets-env --watch"
  },
  "author": "Alex",
  "license": "ISC"
}
